# Shell scripts must stay LF — CRLF would break the `#!/bin/sh` shebang inside the
# Linux container (`bad interpreter: /bin/sh^M`). Critical for entrypoint.sh, which
# the Dockerfile COPYs and runs as ENTRYPOINT. Enforce LF on every checkout/build host.
*.sh text eol=lf
entrypoint.sh text eol=lf

# Dashboard 热更新使用 GitHub 自动生成的源码归档。Git clone 仍保留可编辑的
# 约束文件，但归档只携带带 hash 的发布锁，避免旧更新器把源约束文字调整误判成
# 已安装依赖发生变化。
# 此兼容仅在 requirements.lock.txt 规范化 SHA-256 保持为下列值时安全：
# fdb24053349d8e18a55c3a5afbab8b92cc31d94b69e52359b350ab96b79001c9。
# 修改发布锁前必须移除此规则并提供显式依赖迁移；
# tests/test_update_source_gate.py 会强制校验该不变式。
/requirements.txt export-ignore
